import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Angle.Meter.app',
  appName: 'AngleMeterApp',
  webDir: 'dist/myapp/browser',
};

export default config;
